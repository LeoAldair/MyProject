package com.example.myproject

class User(name: String, last: String, email: String, wishlist: Int, level: Int ){
    val name = name
    val last = last
    val email = email
    val wishlist = wishlist
    val level = level
}